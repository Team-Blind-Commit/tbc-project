# Copy to .env.local and fill in values (never commit .env.local)

# Speech analysis — Whisper + GPT-4o judge feedback
OPENAI_API_KEY=

# Fallback when OpenAI is unavailable
GROQ_API_KEY=

# Judge voice playback (ElevenLabs TTS)
ELEVENLABS_API_KEY=

# Optional until Supabase auth/history is wired team-wide
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
